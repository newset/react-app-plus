import React from "react";
import { ScrollView } from "react-native";
import { View, Text } from "native-base";
import pkg from "../../package.json";

const Setting = () => {
  return <ScrollView style={{ padding: 20 }}></ScrollView>;
};

export default Setting;
